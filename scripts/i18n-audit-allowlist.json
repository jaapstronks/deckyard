{
  "_README": [
    "Accepted exceptions for `node scripts/i18n-audit.js` \u2014 literals that look like",
    "user-facing copy but must not go through t(). Every entry carries a reason.",
    "The audit fails on any hardcoded string NOT listed here, so this file is a",
    "burndown list, not a mute button: shrink it, do not grow it.",
    "Key format: \"<repo-relative file> :: <the literal>\" (line numbers deliberately",
    "excluded so the list survives unrelated edits).",
    "`orphans` works the same way for keys present in en/ but referenced nowhere.",
    "It is empty because the backlog was pruned to zero; an entry belongs there",
    "only for a key that must survive without a visible call site, with the reason."
  ],
  "orphans": {},
  "hardcoded": {
    "client/lib/theme/theme-select.js :: Aa": "Typographic specimen \u2014 the letterforms are the content, not a word to translate.",
    "client/lib/theme/theme-select.js :: ClickNL": "Brand name.",
    "client/views/editor/ai-append.js :: LLM": "Acronym used as-is in every locale.",
    "client/views/editor/editor-form/slide-forms/text-blocks.js :: Block 1": "Seeded block titles for a newly added row \u2014 deck content, not chrome. Same deck-language question as the sample content above.",
    "client/views/editor/editor-form/slide-forms/text-blocks.js :: Block 2": "Seeded block titles for a newly added row \u2014 deck content, not chrome. Same deck-language question as the sample content above.",
    "client/views/editor/editor-form/slide-forms/text-blocks.js :: Block 3": "Seeded block titles for a newly added row \u2014 deck content, not chrome. Same deck-language question as the sample content above.",
    "client/views/editor/export-modal.js :: EN": "Language tag, not prose.",
    "client/views/editor/export-modal.js :: Markdown": "Format name.",
    "client/views/editor/export-modal.js :: NL": "Language tag, not prose.",
    "client/views/editor/image-library/giphy-search.js :: Powered by GIPHY": "Required attribution string \u2014 GIPHY brand guidelines mandate the English wording.",
    "client/views/editor/image-library/unsplash-search.js :: Unsplash": "Brand name.",
    "client/views/editor/modals/settings-modal/language.js :: English (en-GB)": "Language names are shown in their own language, by design.",
    "client/views/editor/modals/settings-modal/language.js :: Nederlands (nl)": "Language names are shown in their own language, by design.",
    "shared/slide-types/types/icon-card-grid-slide/authoring.js :: Collaboration": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/icon-card-grid-slide/authoring.js :: Focus": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/icon-card-grid-slide/authoring.js :: Growth": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/icon-card-grid-slide/authoring.js :: Insight": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/icon-card-grid-slide/authoring.js :: Our Approach": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "client/views/editor/topbar/language-mode.js :: EN": "Language tag, not prose.",
    "client/views/editor/topbar/language-mode.js :: NL": "Language tag, not prose.",
    "client/views/settings/font-editor/google-panel.js :: Raleway:400,700": "Syntax example for the Google Fonts field.",
    "client/views/settings/font-editor/index.js :: Acme Sans": "Placeholder example of a font name.",
    "client/views/settings/slide-type-editor/index.js :: .my-slide {\\n  padding: 2em;\\n}": "CSS syntax example.",
    "client/views/settings/tabs/admin-tab.js :: English (EN-GB)": "Language names are shown in their own language, by design.",
    "client/views/settings/tabs/admin-tab.js :: Nederlands (NL)": "Language names are shown in their own language, by design.",
    "client/views/settings/tabs/analytics-tab.js :: Google Analytics 4": "Product names of the supported analytics providers (Umami, Plausible, Matomo, Google Analytics 4).",
    "client/views/settings/tabs/analytics-tab.js :: Matomo": "Product names of the supported analytics providers (Umami, Plausible, Matomo, Google Analytics 4).",
    "client/views/settings/tabs/analytics-tab.js :: Plausible": "Product names of the supported analytics providers (Umami, Plausible, Matomo, Google Analytics 4).",
    "client/views/settings/tabs/analytics-tab.js :: Umami": "Product names of the supported analytics providers (Umami, Plausible, Matomo, Google Analytics 4).",
    "client/views/settings/tabs/preferences-tab.js :: English": "Language names are shown in their own language, by design.",
    "client/views/settings/tabs/preferences-tab.js :: Nederlands": "Language names are shown in their own language, by design.",
    "client/views/settings/tabs/slide-types-tab/index.js :: Other": "English fallback for CATEGORY_LABELS.other, resolved through t() at render time.",
    "shared/slide-types/types/chapter-title-slide/authoring.js :: Chapter One": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/chart-slide/authoring.js :: Growth Metrics": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/content-slide/authoring.js :: Key Insights": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/countdown-slide/authoring.js :: Break": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/cycle-slide/authoring.js :: Act": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/cycle-slide/authoring.js :: Check": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/cycle-slide/authoring.js :: Do": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/cycle-slide/authoring.js :: Feedback Loop": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/cycle-slide/authoring.js :: Implement changes": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/cycle-slide/authoring.js :: Measure results": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/cycle-slide/authoring.js :: Plan": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/cycle-slide/authoring.js :: Set objectives": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/cycle-slide/authoring.js :: Standardise": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/feedback-slide/authoring.js :: Share your thoughts...": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/follow-invite-slide/authoring.js :: Join the presentation": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/funnel-slide/authoring.js :: 30% engagement": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/funnel-slide/authoring.js :: Awareness": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/funnel-slide/authoring.js :: Consideration": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/funnel-slide/authoring.js :: Conversion": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/funnel-slide/authoring.js :: Interest": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/funnel-slide/authoring.js :: New customers": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/funnel-slide/authoring.js :: Qualified leads": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/funnel-slide/authoring.js :: Sales Funnel": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/funnel-slide/authoring.js :: Website visitors": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/gallery-slide/authoring.js :: Project Highlights": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/image-slide/authoring.js :: Full Image": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/image-slide/authoring.js :: Sample image": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/image-text-slide/authoring.js :: Sample image": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/image-text-slide/authoring.js :: Visual Storytelling": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/kpi-metrics-slide/authoring.js :: Key Metrics": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/list-slide/authoring.js :: Continuous improvement": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/list-slide/authoring.js :: Delivering exceptional results": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/list-slide/authoring.js :: Discovery": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/list-slide/authoring.js :: Execution": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/list-slide/authoring.js :: Our Process": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/list-slide/authoring.js :: Planning the optimal approach": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/list-slide/authoring.js :: Review": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/list-slide/authoring.js :: Strategy": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/list-slide/authoring.js :: Understanding your needs and goals": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/logo-wall-slide/authoring.js :: Acme Corp logo": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/logo-wall-slide/authoring.js :: Bright Labs logo": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/logo-wall-slide/authoring.js :: GlobalNet logo": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/logo-wall-slide/authoring.js :: Innovate Inc logo": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/logo-wall-slide/authoring.js :: Our Partners": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/logo-wall-slide/authoring.js :: Summit Co logo": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/logo-wall-slide/authoring.js :: TechFlow logo": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/payoff-slide/authoring.js :: Thank You": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/pyramid-slide/authoring.js :: Day-to-day actions": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/pyramid-slide/authoring.js :: Foundation": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/pyramid-slide/authoring.js :: How we get there": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/pyramid-slide/authoring.js :: Long-term goals": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/pyramid-slide/authoring.js :: Operations": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/pyramid-slide/authoring.js :: Priority Pyramid": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/pyramid-slide/authoring.js :: Strategy": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/pyramid-slide/authoring.js :: Tactics": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/pyramid-slide/authoring.js :: Vision": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/table-slide/authoring.js :: Quarterly Results": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/team-cards-slide/authoring.js :: Meet the Team": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/text-blocks-slide/authoring.js :: Build": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/text-blocks-slide/authoring.js :: Design": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/text-blocks-slide/authoring.js :: Iterate": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/text-blocks-slide/authoring.js :: Launch": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/text-blocks-slide/authoring.js :: Measure": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/text-blocks-slide/authoring.js :: Our Process": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/text-blocks-slide/authoring.js :: Research": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/text-blocks-slide/authoring.js :: The Result": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/title-slide/authoring.js :: Presentation Title": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up.",
    "shared/slide-types/types/video-slide/authoring.js :: Video Content": "Sample deck content for the slide-type picker thumbnails, not UI chrome. Localising it is a separate call: it seeds slide *content*, which should follow the deck language rather than the UI locale. Tracked as a follow-up."
  }
}
